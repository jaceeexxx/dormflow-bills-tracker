import {icon} from './icons.js';
import {escapeHtml} from './read-model-v3.js';

const service=(route,label,detail,iconName,action='')=>`<button class="service-menu-card" ${action?`data-action="${action}"`:`data-route="${route}"`} type="button"><span class="service-menu-icon">${icon(iconName)}</span><div><strong>${label}</strong><small>${detail}</small></div><b>›</b></button>`;
export function renderMemberMore(identity={}){return `<section class="screen banking-dashboard more-screen"><div class="bank-page-head"><div><span class="screen-kicker">Services</span><h1>More</h1></div><span class="member-status-pill">${escapeHtml(identity.displayName||identity.display_name||'Member')}</span></div><article class="bank-panel"><div class="panel-head"><div><span>Household</span><h2>Financial services</h2></div></div><div class="service-menu-grid">${service('utilities','Utilities','Electricity, water and WiFi','utilities')}${service('expenses','Household expenses','Shared monthly spending','wallet')}${service('paylater','PayLater','Installments and schedules','paylater')}${service('notifications','Notifications','Payment and due reminders','notifications')}</div></article><article class="bank-panel"><div class="panel-head"><div><span>Account</span><h2>Your settings</h2></div></div><div class="settings-card-list">${service('payment-methods','Payment Methods','MariBank accounts and household QR codes','qr')}${service('profile','Profile','Personal details and preferences','users')}${service('security','Security','App lock and device privacy','admin')}</div></article></section>`;}
